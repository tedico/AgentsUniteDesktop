# Field notes — plan 2026-09-10, run 2026-09-11 — Antigravity CLI seat

Branch `feat/agy-seat` @ f7526af (Tasks 1–6 of `docs/superpowers/plans/2026-09-10-agy-seat.md`). Suite 180/180.
Execution: Conductor + Workhorse — Antigravity (`agy`, accept-edits print mode) wrote every test and code file from the plan's briefs; Claude Code ran the red/green steps, the suite, the smokes, the reviews, and the commits. Six implementer runs, 39–161 s each; the longest was the docs task (226k input tokens).

## F1 Fresh room, no notebook (conductor, 07:51)
- Room: `.unite/chats/agy-live` in the worktree, created with `new agy-live`, no `.unite/config.json` (all defaults).
- Preflight: `ok  Claude CLI: /Users/teds/.local/bin/claude`, `ok  Antigravity CLI: /Users/teds/.local/bin/agy`. No Accessibility prompt.
- Model line: `@gemini model: gemini-3.1-pro-high (agy models)`
- Banner: `@claude (CLI, tools on) · @gemini (agy · plan · gemini-3.1-pro-high)`
- Turn: `@gemini reply with the single word pong and nothing else` → status line ran `starting → connected → thinking → replying`; reply `pong` at 10 s.
- state.json after: gemini `sessionRef` `866acbb1-ee1a-438a-aa34-b1f289345390` (an Antigravity conversation id), cursor 2.
- Room files: `state.json`, `transcript.jsonl`, `transcript.md` — no `errors.log`, no `traces.log`.

## F2 Migration of a desktop-era room (conductor, 07:53)
- Copied the main checkout's `main` room (187 messages, three seats in state) to `migrate-check`, set gemini `sessionRef` to `desktop:gemini`, launched with `new migrate-check` and quit.
- Guard line printed: yes — `(@gemini switched to Antigravity — its session was reset; the preamble and the full transcript are re-sent on its next turn)`.
- state.json after: gemini `sessionRef` null, cursor 0. Claude's entry untouched. Room removed afterwards.

## F3 Grounded rounds (Ted)
- Pending. Bind a notebook (`"notebookId"` in the room's `.unite/config.json`; `notebooklm list` prints ids), relaunch `AgentsUniteD`, then: `@gemini <a question the notebook answers>` → one turn with `[N]` citations; `@claude <a question that needs Gemini's reading of the notebook>` → Claude hands off, Gemini answers. Record seconds per turn from the status line, whether citations survived, and whether `errors.log` stayed empty.

## Compared with the desktop seat
- Desktop seat on 2026-09-08: 4–35 s per accessibility poll, 213 s at nine replies, growing with transcript length.
- agy seat: 10 s for a one-word Pro turn (F1); 25 s for a three-bullet cited answer and 15 s for a delta-only follow-up in the 2026-09-10 probe; independent of transcript length. Startup adds one `agy models` call, about 2 s.

## Open
- Per-seat NotebookLM MCP tools (spec §10) — not started; prerequisites listed in the spec.
- Deferred minors from the task reviews, for the whole-branch review: model resolution runs before the spinner (a hung `agy models` shows a blank terminal for up to 15 s); the runner's source-regex test could be joined by a spawn test of the invalid-seat exit; `unite-desktop ls` exits 1 on a room with an invalid `geminiSeat`.
